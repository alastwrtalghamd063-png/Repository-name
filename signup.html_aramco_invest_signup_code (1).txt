<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أرامكو للاستثمار - إنشاء حساب</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        :root {
            --primary: #4d7cfe;
            --primary-gradient: linear-gradient(135deg, #4d7cfe 0%, #2563eb 100%);
            --surface: #f8f9ff;
            --on-surface: #1a1c1e;
            --outline: #e2e8f0;
        }
        body {
            font-family: 'IBM Plex Sans Arabic', sans-serif;
            background-color: var(--surface);
            color: var(--on-surface);
            margin: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .header-bg {
            background: var(--primary-gradient);
            border-radius: 0 0 40px 40px;
        }
        .card {
            background: white;
            border-radius: 24px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03);
            border: 1px solid rgba(226, 232, 240, 0.5);
        }
        .input-group {
            position: relative;
            margin-bottom: 1.25rem;
        }
        .input-field {
            width: 100%;
            padding: 0.875rem 3rem 0.875rem 1rem;
            border-radius: 1rem;
            border: 1px solid #e2e8f0;
            background: #fcfdfe;
            font-size: 0.95rem;
            transition: all 0.2s ease;
        }
        .input-field:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(77, 124, 254, 0.1);
            outline: none;
        }
        .input-icon {
            position: absolute;
            top: 50%;
            right: 1rem;
            transform: translateY(-50%);
            color: #94a3b8;
            font-size: 1.25rem;
        }
        .btn-primary {
            width: 100%;
            padding: 1rem;
            border-radius: 1rem;
            background-color: var(--primary);
            color: white;
            font-weight: 700;
            font-size: 1.1rem;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            transition: all 0.2s ease;
            margin-top: 1.5rem;
            box-shadow: 0 10px 20px rgba(77, 124, 254, 0.2);
        }
        .btn-primary:active {
            transform: scale(0.97);
        }
        .tab-btn {
            flex: 1;
            padding: 0.75rem;
            text-align: center;
            font-weight: 600;
            border-bottom: 2px solid transparent;
            color: #94a3b8;
            transition: all 0.2s;
        }
        .tab-btn.active {
            color: var(--primary);
            border-bottom-color: var(--primary);
        }
    </style>
</head>
<body>

    <!-- Header Section -->
    <header class="header-bg text-white pt-12 pb-20 px-6 relative overflow-hidden">
        <div class="flex justify-between items-center mb-8 relative z-10">
            <div class="flex items-center gap-2">
                <span class="material-symbols-outlined text-3xl">account_balance_wallet</span>
                <h1 class="text-2xl font-bold tracking-tight">Broadcom</h1>
            </div>
            <div class="flex items-center gap-3">
                <button class="bg-white/20 p-2 rounded-full backdrop-blur-md">
                    <span class="material-symbols-outlined text-xl">notifications</span>
                </button>
                <div class="bg-white/20 px-3 py-1 rounded-full backdrop-blur-md flex items-center gap-1 text-sm font-medium">
                    <span class="material-symbols-outlined text-sm">language</span>
                    <span>عربي</span>
                </div>
            </div>
        </div>

        <div class="text-center relative z-10">
            <p class="text-white/80 text-sm mb-1 font-medium">ابدأ رحلتك الاستثمارية اليوم</p>
            <h2 class="text-3xl font-black">إنشاء حساب جديد</h2>
        </div>
        
        <!-- Decoration -->
        <div class="absolute -bottom-10 -left-10 w-40 h-40 bg-white/10 rounded-full blur-3xl"></div>
    </header>

    <main class="px-5 -mt-10 relative z-20 mb-10">
        <div class="card p-6">
            <!-- Tabs -->
            <div class="flex mb-8">
                <button class="tab-btn active">إنشاء حساب</button>
                <a href="login.html" class="tab-btn">تسجيل الدخول</a>
            </div>

            <form onsubmit="handleSignup(event)">
                <div class="input-group">
                    <label class="block text-sm font-bold text-slate-700 mb-2 pr-1">البريد الإلكتروني أو رقم الهاتف</label>
                    <div class="relative">
                        <input type="text" class="input-field" placeholder="user@email.com / +966" required>
                        <span class="material-symbols-outlined input-icon">mail</span>
                    </div>
                </div>

                <div class="input-group">
                    <label class="block text-sm font-bold text-slate-700 mb-2 pr-1">كلمة المرور</label>
                    <div class="relative">
                        <input type="password" id="password" class="input-field" placeholder="أدخل كلمة المرور" required>
                        <span class="material-symbols-outlined input-icon">lock</span>
                    </div>
                </div>

                <div class="input-group">
                    <label class="block text-sm font-bold text-slate-700 mb-2 pr-1">تأكيد كلمة المرور</label>
                    <div class="relative">
                        <input type="password" class="input-field" placeholder="أعد إدخال كلمة المرور" required>
                        <span class="material-symbols-outlined input-icon">lock_reset</span>
                    </div>
                </div>

                <div class="input-group">
                    <label class="block text-sm font-bold text-slate-700 mb-2 pr-1">شفرة الدعوة</label>
                    <div class="relative">
                        <input type="text" id="inviteCodeField" class="input-field bg-slate-50 font-bold" placeholder="كود الدعوة" required>
                        <span class="material-symbols-outlined input-icon">group_add</span>
                    </div>
                </div>

                <button type="submit" class="btn-primary">
                    <span>اشتراك الآن</span>
                    <span class="material-symbols-outlined">arrow_back</span>
                </button>
            </form>

            <p class="mt-8 text-center text-xs text-slate-500 leading-relaxed">
                بالاستمرار، أنت توافق على <a href="#" class="text-blue-600 font-bold underline">شروط الخدمة</a> و <a href="#" class="text-blue-600 font-bold underline">سياسة الخصوصية</a> الخاصة بمنصة أرامكو للاستثمار.
            </p>
        </div>
    </main>

    <script>
        // دالة لجلب شفرة الدعوة من الرابط تلقائياً
        function getInviteCodeFromURL() {
            const urlParams = new URLSearchParams(window.location.search);
            const inviteCode = urlParams.get('invite_code');
            if (inviteCode) {
                document.getElementById('inviteCodeField').value = inviteCode;
            } else {
                // شفرة افتراضية في حال لم تكن موجودة في الرابط
                document.getElementById('inviteCodeField').value = 'r9s3fd';
            }
        }

        // تنفيذ الجلب عند تحميل الصفحة
        window.onload = getInviteCodeFromURL;

        function handleSignup(e) {
            e.preventDefault();
            alert('تم إنشاء الحساب بنجاح! جاري تحويلك للوحة التحكم...');
            window.location.href = 'index.html';
        }
    </script>
</body>
</html>
