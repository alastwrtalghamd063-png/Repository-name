<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أرامكو للاستثمار - نظام الفريق</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        :root {
            --primary: #4d7cfe;
            --primary-gradient: linear-gradient(135deg, #4d7cfe 0%, #2563eb 100%);
            --surface: #f8f9ff;
            --on-surface: #1a1c1e;
        }
        body {
            font-family: 'IBM Plex Sans Arabic', sans-serif;
            background-color: var(--surface);
            color: var(--on-surface);
            margin: 0;
            padding-bottom: 90px;
        }
        .header-bg {
            background: var(--primary-gradient);
            border-radius: 0 0 40px 40px;
        }
        .card {
            background: white;
            border-radius: 24px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03);
            border: 1px solid rgba(226, 232, 240, 0.5);
        }
        .copy-btn:active {
            transform: scale(0.95);
        }
    </style>
</head>
<body>

    <!-- Header Section -->
    <header class="header-bg text-white pt-12 pb-20 px-6 relative overflow-hidden">
        <div class="flex justify-between items-center mb-8 relative z-10">
            <div class="flex items-center gap-2">
                <span class="material-symbols-outlined text-3xl">group</span>
                <h1 class="text-2xl font-bold tracking-tight">Aramco Invest</h1>
            </div>
            <div class="flex items-center gap-3">
                <button class="bg-white/20 p-2 rounded-full backdrop-blur-md">
                    <span class="material-symbols-outlined text-xl">notifications</span>
                </button>
                <div class="bg-white/20 px-3 py-1 rounded-full backdrop-blur-md flex items-center gap-1 text-sm font-medium">
                    <span class="material-symbols-outlined text-sm">language</span>
                    <span>عربي</span>
                </div>
            </div>
        </div>

        <div class="text-center relative z-10">
            <p class="text-white/80 text-sm mb-1 font-medium">شفرة الدعوة الخاصة بك :</p>
            <div class="flex items-center justify-center gap-3">
                <span id="inviteCode" class="text-4xl font-black tracking-[0.2em] drop-shadow-md">r9s3fd</span>
                <button onclick="copyText('r9s3fd', 'تم نسخ الكود')" class="bg-black/30 px-5 py-1.5 rounded-full text-xs font-bold hover:bg-black/40 transition-all copy-btn">نسخ</button>
            </div>
        </div>
    </header>

    <main class="px-5 -mt-10 relative z-20">
        <!-- Referral Link Card -->
        <div class="card p-6 mb-6 text-center">
            <h3 class="text-slate-500 text-sm font-bold mb-4">شارك رابط الإحالة الخاص بك وابدأ في الربح</h3>
            <div class="flex items-center gap-3 bg-slate-50 p-3 rounded-2xl border border-slate-100 mb-4">
                <p id="refLink" class="text-xs text-slate-600 font-mono flex-1 truncate">جاري إنشاء الرابط...</p>
                <button onclick="copyRefLink()" class="bg-black text-white px-5 py-2 rounded-xl text-xs font-bold copy-btn">نسخ</button>
            </div>
            <p class="text-[10px] text-slate-400">عند مشاركة هذا الرابط، سيتم ملء شفرة دعوتك تلقائياً للمسجلين الجدد.</p>
        </div>

        <!-- Levels Info -->
        <div class="space-y-4">
             <div class="card p-4 flex justify-between items-center border-r-4 border-blue-600">
                <div>
                    <p class="text-sm font-bold text-slate-800">المستوى الأول</p>
                    <p class="text-[10px] text-slate-400">عمولة مباشرة</p>
                </div>
                <p class="text-xl font-black text-blue-600">19%</p>
            </div>
            <div class="card p-4 flex justify-between items-center border-r-4 border-purple-600">
                <div>
                    <p class="text-sm font-bold text-slate-800">المستوى الثاني</p>
                    <p class="text-[10px] text-slate-400">عمولة الفريق</p>
                </div>
                <p class="text-xl font-black text-purple-600">5%</p>
            </div>
            <div class="card p-4 flex justify-between items-center border-r-4 border-orange-500">
                <div>
                    <p class="text-sm font-bold text-slate-800">المستوى الثالث</p>
                    <p class="text-[10px] text-slate-400">مكافأة الشبكة</p>
                </div>
                <p class="text-xl font-black text-orange-500">3%</p>
            </div>
        </div>
    </main>

    <!-- Navigation -->
    <nav class="fixed bottom-0 left-0 w-full bg-white border-t border-gray-100 flex justify-around items-center py-3 px-2 z-50 pb-safe">
        <a href="index.html" class="flex flex-col items-center gap-1 text-gray-400">
            <span class="material-symbols-outlined">home</span>
            <span class="text-[10px]">الرئيسية</span>
        </a>
        <a href="tasks.html" class="flex flex-col items-center gap-1 text-gray-400">
            <span class="material-symbols-outlined">assignment</span>
            <span class="text-[10px]">المهام</span>
        </a>
        <a href="team.html" class="flex flex-col items-center gap-1 text-blue-600 font-bold">
            <span class="material-symbols-outlined">group</span>
            <span class="text-[10px]">الفريق</span>
        </a>
        <a href="vip.html" class="flex flex-col items-center gap-1 text-gray-400">
            <span class="material-symbols-outlined">military_tech</span>
            <span class="text-[10px]">VIP</span>
        </a>
        <a href="profile.html" class="flex flex-col items-center gap-1 text-gray-400">
            <span class="material-symbols-outlined">person</span>
            <span class="text-[10px]">حسابي</span>
        </a>
    </nav>

    <script>
        // توليد رابط الإحالة بناءً على الرابط الحالي للموقع
        function generateRefLink() {
            const baseUrl = window.location.origin + window.location.pathname.replace('team.html', 'signup.html');
            const fullLink = `${baseUrl}?invite_code=r9s3fd`;
            document.getElementById('refLink').innerText = fullLink;
            return fullLink;
        }

        function copyRefLink() {
            const link = generateRefLink();
            navigator.clipboard.writeText(link).then(() => {
                alert('تم نسخ رابط الإحالة المربوط بشفرتك بنجاح!');
            });
        }

        function copyText(text, msg) {
            navigator.clipboard.writeText(text).then(() => {
                alert(msg);
            });
        }

        window.onload = generateRefLink;
    </script>
</body>
</html>